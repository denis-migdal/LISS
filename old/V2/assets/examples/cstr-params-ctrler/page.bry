from browser import self

async def main():

    Ctrler = await self.LISS.getControlerCstr('cstr-params-ctrler')

    ctrler = Ctrler.new("Ctrler");
    self.document.body.append( ctrler.host );

from browser import aio
aio.run(main())